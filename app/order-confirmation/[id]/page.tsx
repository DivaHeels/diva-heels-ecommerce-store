import {readOrder} from '@/lib/supabase-server';import {formatMinor,getMarket} from '@/lib/markets';import Link from 'next/link'
export default async function OrderConfirmation({params}:{params:Promise<{id:string}>}){const{id}=await params;const order=await readOrder(id);if(!order)return <main className="mx-auto max-w-3xl px-5 py-24"><h1 className="font-editorial text-5xl">Поръчката не е намерена</h1></main>;const market=getMarket(order.market);const paid=order.payment_status==='paid';return <main className="mx-auto max-w-3xl px-5 py-16 lg:py-24"><p className="text-[10px] uppercase tracking-[.25em] text-[#b79a66]">Diva Heels · {order.public_order_number}</p><h1 className="mt-3 font-editorial text-6xl">Благодарим ви</h1><p className="mt-6 text-sm text-[#666]">{paid?'Плащането е потвърдено.':'Поръчката е получена и плащането се обработва.'}</p><div className="mt-12 space-y-4 border-y border-[#e8e5e1] py-7 text-sm"><p><b>Статус плащане:</b> {order.payment_status}</p><p><b>Имейл:</b> {order.customer_email}</p><p><b>Доставка:</b> {order.shipping_country}, {order.shipping_city}</p><p><b>Общо:</b> {formatMinor(order.total_minor,market)} · Безплатна доставка</p></div><Link href="/shop" className="mt-8 inline-block border-b border-[#111] pb-1 text-sm">Обратно към магазина</Link></main>}
